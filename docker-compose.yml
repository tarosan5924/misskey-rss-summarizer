services:
  bot:
    build: .
    env_file: .env
    environment:
      - CACHE_DB_PATH=${CACHE_DB_PATH:+/app/data/cache.db}
    volumes:
      - bot-data:/app/data
    restart: unless-stopped

volumes:
  bot-data:
