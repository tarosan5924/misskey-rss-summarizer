# Misskey instance hostname
# For production: example.tld (only the hostname)
# For localhost or non-TLS environments: http://localhost:3000
MISSKEY_HOST=example.tld

# Authentication token (must have posting permissions)
AUTH_TOKEN=your_auth_token_here


# ---- RSS URL Configuration ----
# Two methods to specify RSS feed URLs:
# Recommended: Use numbered format (RSS_URL_1, RSS_URL_2, ...)
# Legacy comma-separated format is also supported for backward compatibility.
# Note: If a number is skipped, all subsequent URLs are ignored.
# _FILTER Settings:
# - true: The bot will strictly scan the title/content for keywords (e.g., "マユリカ", "エバース").
# - false: The bot will post all entries from the feed without keyword checking.
RSS_URL_1=https://example.tld/rss/hoge.xml
RSS_URL_1_FILTER=false
RSS_URL_2=https://example.tld/rss/fuga.xml
RSS_URL_2_FILTER=false
RSS_URL_3=https://blog.example.tld/feed
RSS_URL_3_FILTER=false

# Separate keywords with commas (No spaces between commas!)
SEARCH_KEYWORDS=マユリカ,エバース,うなげろりん

# Legacy format (comma-separated)
# Note: Numbered format takes priority.
# If both are set, the numbered format will be used.
# RSS_URL=https://example.tld/rss1,https://example.tld/rss2,https://example.tld/rss3



# ---- Optional Settings ----
# RSS fetch interval (seconds)
# Default: 30
# FETCH_INTERVAL=30

# Rate limit: Maximum burst of consecutive posts
# Default: 3
# MAX_PERMITS=3

# Rate limit: Permit refill interval (seconds)
# When exceeding max burst, one post becomes available every N seconds.
# Default: 10
# REFILL_INTERVAL=10

# Post only local server (Default: false)
# LOCAL_ONLY=true


# ---- Cache Settings ----
# SQLite database path for persistent cache
# When set, feed state is preserved across restarts
# Default: empty (in-memory cache)
# CACHE_DB_PATH=./cache.db

# Post only the latest entry on first run (Default: true)
# Set to false to post all unprocessed entries (requires CACHE_DB_PATH)
# FIRST_RUN_LATEST_ONLY=true

# Cache cleanup interval in hours (Default: 24)
# How often to run the cleanup process
# Only applies when CACHE_DB_PATH is set
# CACHE_CLEANUP_INTERVAL=24

# Cache retention period in days (Default: 7)
# Processed GUIDs older than this are deleted during cleanup
# Set this longer than your RSS feed's longest update interval
# CACHE_RETENTION_DAYS=7


# ---- LLM Summarization Settings ----
# LLM provider: "gemini" or empty to disable summarization
# Default: empty (disabled)
# LLM_PROVIDER=gemini

# API key for the LLM provider
# Required when LLM_PROVIDER is set
# LLM_API_KEY=your_gemini_api_key_here

# Model name (required when LLM_PROVIDER is set)
# Check the provider's documentation for available models
# LLM_MODEL=gemini-2.0-flash-exp

# Maximum tokens for summary output
# Set to 0 or leave empty to use model's default (recommended)
# Default: 0 (no limit)
# LLM_MAX_TOKENS=0

# API timeout in seconds
# Default: 30
# LLM_TIMEOUT=30

# Custom system instruction (optional)
# Default: See internal/infrastructure/llm/summarizer.go
# LLM_SYSTEM_INSTRUCTION=あなたは記事要約の専門家です。以下の記事を3文で要約してください。
